// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type Options as Options2, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { AcceptQueryData, AcceptQueryErrors, AcceptQueryResponses, CreateAdminConnectionData, CreateAdminConnectionErrors, CreateAdminConnectionResponses, CreateGroupMappingData, CreateGroupMappingErrors, CreateGroupMappingResponses, CreateRoleData, CreateRoleErrors, CreateRoleResponses, CreateSessionData, CreateSessionErrors, CreateSessionResponses, CreateSsoProviderData, CreateSsoProviderErrors, CreateSsoProviderResponses, DeleteAdminConnectionData, DeleteAdminConnectionErrors, DeleteAdminConnectionResponses, DeleteGroupMappingData, DeleteGroupMappingErrors, DeleteGroupMappingResponses, DeleteHistoryEntryData, DeleteHistoryEntryErrors, DeleteHistoryEntryResponses, DeleteQuotaData, DeleteQuotaErrors, DeleteQuotaResponses, DeleteRoleData, DeleteRoleErrors, DeleteRoleResponses, DeleteSessionData, DeleteSessionErrors, DeleteSessionResponses, DeleteSsoProviderData, DeleteSsoProviderErrors, DeleteSsoProviderResponses, DisableAdminConnectionData, DisableAdminConnectionErrors, DisableAdminConnectionResponses, EnableAdminConnectionData, EnableAdminConnectionErrors, EnableAdminConnectionResponses, ExportAuditEntriesData, ExportAuditEntriesErrors, ExportAuditEntriesResponses, GetAdminConnectionData, GetAdminConnectionErrors, GetAdminConnectionResponses, GetAdminConnectionSchemaData, GetAdminConnectionSchemaErrors, GetAdminConnectionSchemaResponses, GetAdminSettingsData, GetAdminSettingsErrors, GetAdminSettingsResponses, GetAuditRetentionData, GetAuditRetentionErrors, GetAuditRetentionResponses, GetAuditStatusData, GetAuditStatusErrors, GetAuditStatusResponses, GetDetectionConfigData, GetDetectionConfigErrors, GetDetectionConfigResponses, GetHealthData, GetHealthResponses, GetHistoryEntryData, GetHistoryEntryErrors, GetHistoryEntryResponses, GetMeData, GetMeErrors, GetMeResponses, GetQueryLimitsData, GetQueryLimitsErrors, GetQueryLimitsResponses, GetQuotaData, GetQuotaErrors, GetQuotaResponses, GetQuotaStatusData, GetQuotaStatusErrors, GetQuotaStatusResponses, GetReadinessData, GetReadinessErrors, GetReadinessResponses, GetRoleData, GetRoleErrors, GetRoleResponses, GetSessionData, GetSessionErrors, GetSessionResponses, GetSessionsData, GetSessionsErrors, GetSessionsResponses, ListAdminConnectionsData, ListAdminConnectionsErrors, ListAdminConnectionsResponses, ListAdminSsoProvidersData, ListAdminSsoProvidersErrors, ListAdminSsoProvidersResponses, ListGroupMappingsData, ListGroupMappingsErrors, ListGroupMappingsResponses, ListHistoryData, ListHistoryErrors, ListHistoryResponses, ListQuotasData, ListQuotasErrors, ListQuotasResponses, ListRolesData, ListRolesErrors, ListRolesResponses, ListSsoProvidersData, ListSsoProvidersErrors, ListSsoProvidersResponses, ListUserConnectionsData, ListUserConnectionsErrors, ListUserConnectionsResponses, OidcCallbackData, OidcCallbackErrors, OidcLoginData, RefreshAdminConnectionSchemaData, RefreshAdminConnectionSchemaErrors, RefreshAdminConnectionSchemaResponses, RefreshSchemaData, RefreshSchemaErrors, RefreshSchemaResponses, RegenerateQueryData, RegenerateQueryErrors, RegenerateQueryResponses, RejectQueryData, RejectQueryErrors, RejectQueryResponses, SamlCallbackData, SamlCallbackErrors, SamlLoginData, SearchAuditEntriesData, SearchAuditEntriesErrors, SearchAuditEntriesResponses, SignInData, SignInErrors, SignInResponses, SignOutData, SignOutErrors, SignOutResponses, SubmitQuestionData, SubmitQuestionErrors, SubmitQuestionResponses, TestAdminConnectionData, TestAdminConnectionErrors, TestAdminConnectionResponses, TestDraftRolePolicyData, TestDraftRolePolicyErrors, TestDraftRolePolicyResponses, TestRolePolicyData, TestRolePolicyErrors, TestRolePolicyResponses, UpdateAdminConnectionData, UpdateAdminConnectionErrors, UpdateAdminConnectionResponses, UpdateAdminSettingsData, UpdateAdminSettingsErrors, UpdateAdminSettingsResponses, UpdateDetectionConfigData, UpdateDetectionConfigErrors, UpdateDetectionConfigResponses, UpdateFeedbackData, UpdateFeedbackErrors, UpdateFeedbackResponses, UpdateRoleData, UpdateRoleErrors, UpdateRoleResponses, UpdateSessionConnectionData, UpdateSessionConnectionErrors, UpdateSessionConnectionResponses, UpdateSsoProviderData, UpdateSsoProviderErrors, UpdateSsoProviderResponses, UpsertQuotaData, UpsertQuotaErrors, UpsertQuotaResponses, VerifyAuditChainData, VerifyAuditChainErrors, VerifyAuditChainResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Search Audit Entries
 *
 * GET /admin/audit/entries — filtered, paginated audit log search.
 *
 * Permission: ``admin.audit.verify`` (existing Phase 5 permission).
 *
 * Behaviour:
 * 1. Parse filter params from query string.
 * 2. Enforce retention window server-side (entries older than
 * ``AUDIT_RETENTION_MONTHS`` are excluded before pagination).
 * 3. Return ``AuditSearchResponse`` with paginated entries. Results use
 * ``timestamp DESC, sequence_number DESC`` ordering and are bounded by
 * the maximum sequence present when that request starts. Because this is
 * stateless offset pagination, separate page requests made while new
 * entries arrive can still observe normal offset drift.
 * 4. Emit ``AUDIT_SEARCH`` audit event whose context contains ONLY:
 * - sanitized filter summary (param names + values, no result content)
 * - pagination metadata (page, page_size)
 * Never log returned entry values in the audit context.
 *
 * Response shape: ``AuditSearchResponse``
 * (``entries`` list + ``pagination`` metadata).
 */
export const searchAuditEntries = <ThrowOnError extends boolean = false>(options?: Options<SearchAuditEntriesData, ThrowOnError>) => (options?.client ?? client).get<SearchAuditEntriesResponses, SearchAuditEntriesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/audit/entries',
    ...options
});

/**
 * Export Audit Entries
 *
 * POST /admin/audit/export — export filtered audit entries as CSV or JSON.
 *
 * Permission: ``admin.audit.verify`` (existing Phase 5 permission).
 *
 * Behaviour:
 * 1. Check daily export quota via QuotaService.check_and_increment(user_id, role_id, "exports").
 * - QuotaExceededError → 429 with message_key.
 * - QuotaUnavailableError → 503 with message_key (fail-closed).
 * 2. Parse AuditExportRequest from typed FastAPI body param (Pydantic-validated — 422 on bad input).
 * 3. Query entries via AuditSearchService (enforces retention window).
 * 4. If filtered count > 50,000 → 422 with message_key.
 * 5. Serialize via AuditExportService (CSV or JSON with redaction + integrity metadata).
 * 6. Emit AUDIT_EXPORT audit event with only filter_summary and record_count.
 * Never include exported entry values in the context.
 * 7. Return file response with correct Content-Type and Content-Disposition headers.
 *
 * Security constraints:
 * - No exported entry values in AUDIT_EXPORT context.
 * - No stack traces, raw SQL, driver errors, or secrets in any response.
 */
export const exportAuditEntries = <ThrowOnError extends boolean = false>(options: Options<ExportAuditEntriesData, ThrowOnError>) => (options.client ?? client).post<ExportAuditEntriesResponses, ExportAuditEntriesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/audit/export',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Audit Retention
 *
 * GET /admin/audit/retention — return retention policy and last purge summary.
 *
 * Permission: ``admin.audit.verify``.
 *
 * Response 200::
 *
 * {
 * "retention_months": 24,
 * "last_purge_at": "2026-06-01T03:00:00+00:00",  # or null
 * "purged_count": 1500                            # or null
 * }
 *
 * Fields:
 * - ``retention_months``: the configured minimum retention window from
 * ``Settings.AUDIT_RETENTION_MONTHS`` (FR-142).
 * - ``last_purge_at``: ISO-8601 timestamp of the most recent
 * ``audit.purge`` marker row, or ``null`` when no purge has run.
 * - ``purged_count``: the ``purged_count`` value stored in the most
 * recent ``audit.purge`` marker context, or ``null`` when no purge
 * has run.
 *
 * Scheduler timing (e.g. next_purge_at, cron schedule) is deliberately
 * excluded — it is an external operational concern, not surfaced here.
 */
export const getAuditRetention = <ThrowOnError extends boolean = false>(options?: Options<GetAuditRetentionData, ThrowOnError>) => (options?.client ?? client).get<GetAuditRetentionResponses, GetAuditRetentionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/audit/retention',
    ...options
});

/**
 * Get Audit Status
 *
 * GET /admin/audit/status — return durable last verification + entry count.
 *
 * Permission: ``admin.audit.verify``.
 *
 * Both ``total_entries`` and ``last_verification`` are read from the
 * ``audit_log_entries`` table on every request. The endpoint holds
 * no in-process state and survives process restarts.
 *
 * Response 200::
 *
 * {
 * "total_entries": 15235,
 * "last_verification": {
 * "verified": true,
 * "entries_checked": 15234,
 * "first_break_at": null,
 * "verified_at": "2026-06-06T12:00:00+00:00"
 * }
 * }
 *
 * - ``total_entries`` is the actual durable row count in
 * ``audit_log_entries`` (the count INCLUDES any persisted
 * ``audit.verify`` rows). This is process-restart safe.
 * - ``last_verification`` is reconstructed from the most recent
 * ``audit.verify`` row. ``entries_checked`` here reflects the
 * PRE-log count captured at verify time (it is the count the
 * verify endpoint itself returned, not the post-log count).
 * If no verify has ever been performed, ``last_verification``
 * is ``null``.
 *
 * Or, when the audit log is empty::
 *
 * {
 * "total_entries": 0,
 * "last_verification": null
 * }
 *
 * Or, when the audit log has rows but no verify has been performed::
 *
 * {
 * "total_entries": 42,
 * "last_verification": null
 * }
 */
export const getAuditStatus = <ThrowOnError extends boolean = false>(options?: Options<GetAuditStatusData, ThrowOnError>) => (options?.client ?? client).get<GetAuditStatusResponses, GetAuditStatusErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/audit/status',
    ...options
});

/**
 * Verify Audit Chain
 *
 * POST /admin/audit/verify — walk the audit chain and emit AUDIT_VERIFY.
 *
 * Permission: ``admin.audit.verify``.
 *
 * Behaviour:
 * 1. Walk the chain via ``AuditService.verify_chain`` (PRE-log count).
 * 2. Append an ``audit.verify`` audit entry to the chain. The
 * ``outcome`` is ``"success"`` when the chain is intact and
 * ``"broken"`` when ``first_break_at`` is not None. The
 * ``context`` carries only ``verified`` (bool), ``entries_checked``
 * (int), and ``first_break_at`` (int | None) — no raw chain rows,
 * no SQL, no schema internals, no driver errors.
 *
 * S-008 chain recovery:
 * - The endpoint never mutates or rewrites chain rows. No auto-repair.
 * - On a broken chain, ``first_break_at`` carries the
 * ``sequence_number`` of the first mismatch; admin decides recovery.
 * - After a break, the next ``AuditService.log`` call appends with
 * ``next_seq = (last_seq or 0) + 1``. The chain restarts from the
 * last row's ``row_hash`` regardless of the break.
 *
 * Response 200: ``AuditVerifyResponse`` shape
 * (``verified``, ``entries_checked``, ``first_break_at``, ``verified_at``).
 */
export const verifyAuditChain = <ThrowOnError extends boolean = false>(options?: Options<VerifyAuditChainData, ThrowOnError>) => (options?.client ?? client).post<VerifyAuditChainResponses, VerifyAuditChainErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/audit/verify',
    ...options
});

/**
 * List Connections
 *
 * GET /admin/connections — list all source database connections.
 *
 * Requires ``admin.connections.manage`` permission.
 */
export const listAdminConnections = <ThrowOnError extends boolean = false>(options?: Options<ListAdminConnectionsData, ThrowOnError>) => (options?.client ?? client).get<ListAdminConnectionsResponses, ListAdminConnectionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections',
    ...options
});

/**
 * Create Connection
 *
 * POST /admin/connections — create a new source database connection.
 *
 * Requires ``admin.connections.manage`` permission. Emits a
 * ``connection.create`` audit entry before returning.
 */
export const createAdminConnection = <ThrowOnError extends boolean = false>(options: Options<CreateAdminConnectionData, ThrowOnError>) => (options.client ?? client).post<CreateAdminConnectionResponses, CreateAdminConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Connection
 *
 * DELETE /admin/connections/{id} — hard-delete a connection (blocked if referenced).
 *
 * Requires ``admin.connections.manage`` permission. Emits a
 * ``connection.delete`` audit entry before returning.
 */
export const deleteAdminConnection = <ThrowOnError extends boolean = false>(options: Options<DeleteAdminConnectionData, ThrowOnError>) => (options.client ?? client).delete<DeleteAdminConnectionResponses, DeleteAdminConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections/{connection_id}',
    ...options
});

/**
 * Get Connection
 *
 * GET /admin/connections/{id} — get a connection by ID.
 *
 * Requires ``admin.connections.manage`` permission.
 */
export const getAdminConnection = <ThrowOnError extends boolean = false>(options: Options<GetAdminConnectionData, ThrowOnError>) => (options.client ?? client).get<GetAdminConnectionResponses, GetAdminConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections/{connection_id}',
    ...options
});

/**
 * Update Connection
 *
 * PUT /admin/connections/{id} — update an existing connection.
 *
 * Requires ``admin.connections.manage`` permission. Emits a
 * ``connection.update`` audit entry before returning.
 */
export const updateAdminConnection = <ThrowOnError extends boolean = false>(options: Options<UpdateAdminConnectionData, ThrowOnError>) => (options.client ?? client).put<UpdateAdminConnectionResponses, UpdateAdminConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections/{connection_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Disable Connection
 *
 * POST /admin/connections/{id}/disable — disable an active connection.
 *
 * Requires ``admin.connections.manage`` permission.
 */
export const disableAdminConnection = <ThrowOnError extends boolean = false>(options: Options<DisableAdminConnectionData, ThrowOnError>) => (options.client ?? client).post<DisableAdminConnectionResponses, DisableAdminConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections/{connection_id}/disable',
    ...options
});

/**
 * Enable Connection
 *
 * POST /admin/connections/{id}/enable — re-enable a disabled connection.
 *
 * Requires ``admin.connections.manage`` permission.
 */
export const enableAdminConnection = <ThrowOnError extends boolean = false>(options: Options<EnableAdminConnectionData, ThrowOnError>) => (options.client ?? client).post<EnableAdminConnectionResponses, EnableAdminConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections/{connection_id}/enable',
    ...options
});

/**
 * Refresh Schema
 *
 * POST /admin/connections/{id}/refresh-schema — trigger schema introspection.
 *
 * Requires ``admin.connections.manage`` permission.
 */
export const refreshAdminConnectionSchema = <ThrowOnError extends boolean = false>(options: Options<RefreshAdminConnectionSchemaData, ThrowOnError>) => (options.client ?? client).post<RefreshAdminConnectionSchemaResponses, RefreshAdminConnectionSchemaErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections/{connection_id}/refresh-schema',
    ...options
});

/**
 * Get Schema
 *
 * GET /admin/connections/{id}/schema — get introspected schema summary.
 *
 * Contract (T-742, Wave 17.3n): accepts either
 * ``admin.connections.manage`` (original) or ``admin.roles.manage`` (added
 * so the role policy editor's schema browser works for roles-only
 * admins). All other permissions still 403 with the standard
 * sanitized ``error.forbidden`` payload. The endpoint does NOT echo
 * the connection id, the user's role id, or the requested permission
 * set. No response shape change.
 */
export const getAdminConnectionSchema = <ThrowOnError extends boolean = false>(options: Options<GetAdminConnectionSchemaData, ThrowOnError>) => (options.client ?? client).get<GetAdminConnectionSchemaResponses, GetAdminConnectionSchemaErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections/{connection_id}/schema',
    ...options
});

/**
 * Test Connection
 *
 * POST /admin/connections/{id}/test — test a connection's health.
 *
 * Requires ``admin.connections.manage`` permission.
 */
export const testAdminConnection = <ThrowOnError extends boolean = false>(options: Options<TestAdminConnectionData, ThrowOnError>) => (options.client ?? client).post<TestAdminConnectionResponses, TestAdminConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/connections/{connection_id}/test',
    ...options
});

/**
 * Get Detection Config
 *
 * Return current detection threshold configuration.
 */
export const getDetectionConfig = <ThrowOnError extends boolean = false>(options?: Options<GetDetectionConfigData, ThrowOnError>) => (options?.client ?? client).get<GetDetectionConfigResponses, GetDetectionConfigErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/detection/config',
    ...options
});

/**
 * Update Detection Config
 *
 * Update detection threshold configuration and emit audit event.
 *
 * FastAPI parses the body as DetectionThresholdUpdate, which validates
 * block_confidence > flag_confidence via a Pydantic model_validator.
 * Invalid input → FastAPI automatically returns 422 (RequestValidationError).
 * Emits DETECTION_CONFIG_CHANGE audit event with sanitized context.
 */
export const updateDetectionConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateDetectionConfigData, ThrowOnError>) => (options.client ?? client).put<UpdateDetectionConfigResponses, UpdateDetectionConfigErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/detection/config',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Quotas
 */
export const listQuotas = <ThrowOnError extends boolean = false>(options?: Options<ListQuotasData, ThrowOnError>) => (options?.client ?? client).get<ListQuotasResponses, ListQuotasErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/quotas',
    ...options
});

/**
 * Get Quota Status
 */
export const getQuotaStatus = <ThrowOnError extends boolean = false>(options?: Options<GetQuotaStatusData, ThrowOnError>) => (options?.client ?? client).get<GetQuotaStatusResponses, GetQuotaStatusErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/quotas/status',
    ...options
});

/**
 * Delete Quota
 *
 * Delete a quota or reconcile a committed deletion before returning 204.
 *
 * A pending 503 is retryable and distinct from the preserved 404 response
 * for a genuinely unknown, non-pending quota configuration.
 */
export const deleteQuota = <ThrowOnError extends boolean = false>(options: Options<DeleteQuotaData, ThrowOnError>) => (options.client ?? client).delete<DeleteQuotaResponses, DeleteQuotaErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/quotas/{role_id}',
    ...options
});

/**
 * Get Quota
 */
export const getQuota = <ThrowOnError extends boolean = false>(options: Options<GetQuotaData, ThrowOnError>) => (options.client ?? client).get<GetQuotaResponses, GetQuotaErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/quotas/{role_id}',
    ...options
});

/**
 * Upsert Quota
 *
 * Apply a quota update or return a sanitized synchronization-pending 503.
 *
 * A pending response means PostgreSQL and its success audit committed once,
 * while quota enforcement remains fail closed until an identical retry
 * publishes the authoritative configuration.
 */
export const upsertQuota = <ThrowOnError extends boolean = false>(options: Options<UpsertQuotaData, ThrowOnError>) => (options.client ?? client).put<UpsertQuotaResponses, UpsertQuotaErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/quotas/{role_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Refresh Schema
 *
 * POST /admin/refresh-schema — force a schema introspection cache refresh.
 *
 * Requires X-Admin-Key header unconditionally (G-006).
 *
 * Returns tables_count, columns_count, approximate_tokens, refreshed_at.
 */
export const refreshSchema = <ThrowOnError extends boolean = false>(options?: Options<RefreshSchemaData, ThrowOnError>) => (options?.client ?? client).post<RefreshSchemaResponses, RefreshSchemaErrors, ThrowOnError>({
    security: [{ name: 'X-Admin-Key', type: 'apiKey' }],
    url: '/api/v1/admin/refresh-schema',
    ...options
});

/**
 * List Roles
 *
 * GET /admin/roles — list all roles.
 */
export const listRoles = <ThrowOnError extends boolean = false>(options?: Options<ListRolesData, ThrowOnError>) => (options?.client ?? client).get<ListRolesResponses, ListRolesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/roles',
    ...options
});

/**
 * Create Role
 *
 * POST /admin/roles — create a new role.
 */
export const createRole = <ThrowOnError extends boolean = false>(options: Options<CreateRoleData, ThrowOnError>) => (options.client ?? client).post<CreateRoleResponses, CreateRoleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/roles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Test Draft Role Policy
 *
 * Validate and evaluate one supplied policy without persistence or execution.
 */
export const testDraftRolePolicy = <ThrowOnError extends boolean = false>(options: Options<TestDraftRolePolicyData, ThrowOnError>) => (options.client ?? client).post<TestDraftRolePolicyResponses, TestDraftRolePolicyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/roles/test-policy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Role
 *
 * DELETE /admin/roles/{id} — remove a role.
 */
export const deleteRole = <ThrowOnError extends boolean = false>(options: Options<DeleteRoleData, ThrowOnError>) => (options.client ?? client).delete<DeleteRoleResponses, DeleteRoleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/roles/{role_id}',
    ...options
});

/**
 * Get Role
 *
 * GET /admin/roles/{id} — get role detail.
 */
export const getRole = <ThrowOnError extends boolean = false>(options: Options<GetRoleData, ThrowOnError>) => (options.client ?? client).get<GetRoleResponses, GetRoleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/roles/{role_id}',
    ...options
});

/**
 * Update Role
 *
 * PUT /admin/roles/{id} — update a role.
 */
export const updateRole = <ThrowOnError extends boolean = false>(options: Options<UpdateRoleData, ThrowOnError>) => (options.client ?? client).put<UpdateRoleResponses, UpdateRoleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/roles/{role_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Test Role Policy
 *
 * POST /admin/roles/{id}/test-policy — dry-run a question against role policy.
 *
 * Request body: ``{"question": str, "connection_id": uuid}``.
 *
 * Response 200::
 *
 * {
 * "accessible_tables": [...],
 * "accessible_columns": {"table": [cols]},
 * "blocked_tables": [...],
 * "applicable_row_filters": [{"table": ..., "filter": ...}],
 * "masked_columns": {"table": [cols]},
 * "would_be_allowed": bool
 * }
 *
 * Behaviour:
 * - Loads the role by id. Unknown / malformed role id -> sanitized
 * 404 with ``error.notFound``. The id is never echoed.
 * - Loads the connection by id. Unknown / malformed connection id,
 * or inactive / unhealthy / no-schema connection -> sanitized 400
 * with the matching connection_* message key. Credentials, host,
 * port, username, and the raw id are never echoed.
 * - Loads the ``role_connection_policies`` row for
 * ``(role_id, connection_id)``. Missing row -> deny-all result
 * (consistent with PR #129 fail-closed provider; a role with no
 * policy for the connection sees nothing).
 * - Applies ``PolicyEnforcementService.filter_schema`` to the
 * connection's introspected schema with the policy's
 * ``allowed_tables``. The result drives
 * ``accessible_tables`` / ``accessible_columns``.
 * - ``blocked_tables`` = every schema table not in accessible.
 * - ``applicable_row_filters`` is the policy's row-filter list
 * echoed verbatim — never interpolated, never bound. Placeholder
 * syntax (``{user.*}``) is preserved as metadata.
 * - ``masked_columns`` is derived from the policy's ``column_masks``
 * and includes only columns that also appear in
 * ``accessible_columns`` (a mask on a non-accessible column is
 * not a leak risk but is omitted for clarity).
 * - ``would_be_allowed`` is True iff the policy grants at least one
 * table. The endpoint does NOT evaluate generated SQL — that is
 * the role of the live ``/query/submit`` evaluator; this dry-run
 * is a policy-state preview, not a query simulation.
 * - Internal failures (driver errors, missing tables, etc.) are
 * caught and returned as sanitized 500 with constant
 * ``error.internal``. No host / port / username / SQL / stack
 * trace / driver class leaks in any error path.
 * - Inputs (schema entries, allowed_tables, row_filters,
 * column_masks) are never mutated.
 * - The question field is accepted for context but is not used to
 * drive any LLM call or SQL generation.
 */
export const testRolePolicy = <ThrowOnError extends boolean = false>(options: Options<TestRolePolicyData, ThrowOnError>) => (options.client ?? client).post<TestRolePolicyResponses, TestRolePolicyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/roles/{role_id}/test-policy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Settings Admin
 *
 * GET /admin/settings — retrieve admin settings via session cookie.
 *
 * Requires ``admin.connections.manage`` permission.
 */
export const getAdminSettings = <ThrowOnError extends boolean = false>(options?: Options<GetAdminSettingsData, ThrowOnError>) => (options?.client ?? client).get<GetAdminSettingsResponses, GetAdminSettingsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/settings',
    ...options
});

/**
 * Update Settings Admin
 *
 * PATCH /admin/settings — update admin settings via session cookie.
 *
 * Requires ``admin.connections.manage`` permission. Emits an
 * ``admin.config.change`` audit entry before returning. Audit
 * failures propagate (fail-closed) per the project-wide contract.
 */
export const updateAdminSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateAdminSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateAdminSettingsResponses, UpdateAdminSettingsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Group Mappings
 *
 * GET /admin/sso/group-mappings — list all SSO group-to-role mappings.
 */
export const listGroupMappings = <ThrowOnError extends boolean = false>(options?: Options<ListGroupMappingsData, ThrowOnError>) => (options?.client ?? client).get<ListGroupMappingsResponses, ListGroupMappingsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/sso/group-mappings',
    ...options
});

/**
 * Create Group Mapping
 *
 * POST /admin/sso/group-mappings — create a new group-to-role mapping.
 */
export const createGroupMapping = <ThrowOnError extends boolean = false>(options: Options<CreateGroupMappingData, ThrowOnError>) => (options.client ?? client).post<CreateGroupMappingResponses, CreateGroupMappingErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/sso/group-mappings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Group Mapping
 *
 * DELETE /admin/sso/group-mappings/{id} — remove a group-to-role mapping.
 */
export const deleteGroupMapping = <ThrowOnError extends boolean = false>(options: Options<DeleteGroupMappingData, ThrowOnError>) => (options.client ?? client).delete<DeleteGroupMappingResponses, DeleteGroupMappingErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/sso/group-mappings/{mapping_id}',
    ...options
});

/**
 * List Providers
 *
 * GET /admin/sso/providers — list all providers with masked secrets.
 */
export const listAdminSsoProviders = <ThrowOnError extends boolean = false>(options?: Options<ListAdminSsoProvidersData, ThrowOnError>) => (options?.client ?? client).get<ListAdminSsoProvidersResponses, ListAdminSsoProvidersErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/sso/providers',
    ...options
});

/**
 * Create Provider
 *
 * POST /admin/sso/providers — create new provider, encrypt secrets.
 */
export const createSsoProvider = <ThrowOnError extends boolean = false>(options: Options<CreateSsoProviderData, ThrowOnError>) => (options.client ?? client).post<CreateSsoProviderResponses, CreateSsoProviderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/sso/providers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Provider
 *
 * DELETE /admin/sso/providers/{id} — remove provider.
 */
export const deleteSsoProvider = <ThrowOnError extends boolean = false>(options: Options<DeleteSsoProviderData, ThrowOnError>) => (options.client ?? client).delete<DeleteSsoProviderResponses, DeleteSsoProviderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/sso/providers/{provider_id}',
    ...options
});

/**
 * Update Provider
 *
 * PUT /admin/sso/providers/{id} — partial update, encrypt new secrets.
 */
export const updateSsoProvider = <ThrowOnError extends boolean = false>(options: Options<UpdateSsoProviderData, ThrowOnError>) => (options.client ?? client).put<UpdateSsoProviderResponses, UpdateSsoProviderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/admin/sso/providers/{provider_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Me
 *
 * GET /auth/me — return current user profile.
 */
export const getMe = <ThrowOnError extends boolean = false>(options?: Options<GetMeData, ThrowOnError>) => (options?.client ?? client).get<GetMeResponses, GetMeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/auth/me',
    ...options
});

/**
 * Sign In
 *
 * POST /auth/sign-in — authenticate and set session cookie.
 */
export const signIn = <ThrowOnError extends boolean = false>(options: Options<SignInData, ThrowOnError>) => (options.client ?? client).post<SignInResponses, SignInErrors, ThrowOnError>({
    url: '/api/v1/auth/sign-in',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Sign Out
 *
 * POST /auth/sign-out — delete session and clear cookie.
 */
export const signOut = <ThrowOnError extends boolean = false>(options?: Options<SignOutData, ThrowOnError>) => (options?.client ?? client).post<SignOutResponses, SignOutErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/auth/sign-out',
    ...options
});

/**
 * Oidc Callback
 *
 * GET /auth/sso/oidc/callback — process OIDC callback.
 */
export const oidcCallback = <ThrowOnError extends boolean = false>(options: Options<OidcCallbackData, ThrowOnError>) => (options.client ?? client).get<unknown, OidcCallbackErrors, ThrowOnError>({ url: '/api/v1/auth/sso/oidc/callback', ...options });

/**
 * Oidc Login
 *
 * GET /auth/sso/oidc/login — initiate OIDC authorization code flow.
 */
export const oidcLogin = <ThrowOnError extends boolean = false>(options?: Options<OidcLoginData, ThrowOnError>) => (options?.client ?? client).get<unknown, unknown, ThrowOnError>({ url: '/api/v1/auth/sso/oidc/login', ...options });

/**
 * List Providers
 *
 * GET /auth/sso/providers — list configured SSO providers for sign-in page.
 */
export const listSsoProviders = <ThrowOnError extends boolean = false>(options?: Options<ListSsoProvidersData, ThrowOnError>) => (options?.client ?? client).get<ListSsoProvidersResponses, ListSsoProvidersErrors, ThrowOnError>({ url: '/api/v1/auth/sso/providers', ...options });

/**
 * Saml Callback
 *
 * POST /auth/sso/saml/callback — process SAML assertion (ACS endpoint).
 */
export const samlCallback = <ThrowOnError extends boolean = false>(options: Options<SamlCallbackData, ThrowOnError>) => (options.client ?? client).post<unknown, SamlCallbackErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/api/v1/auth/sso/saml/callback',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Saml Login
 *
 * GET /auth/sso/saml/login — initiate SAML AuthnRequest.
 */
export const samlLogin = <ThrowOnError extends boolean = false>(options?: Options<SamlLoginData, ThrowOnError>) => (options?.client ?? client).get<unknown, unknown, ThrowOnError>({ url: '/api/v1/auth/sso/saml/login', ...options });

/**
 * List User Connections
 *
 * GET /connections — list connections available for user query selection.
 *
 * Returns only active + healthy + successfully introspected connections.
 * Minimal payload: id, display_name, database_type. No host/port/credentials.
 */
export const listUserConnections = <ThrowOnError extends boolean = false>(options?: Options<ListUserConnectionsData, ThrowOnError>) => (options?.client ?? client).get<ListUserConnectionsResponses, ListUserConnectionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/connections',
    ...options
});

/**
 * Update Feedback
 *
 * PATCH /feedback/:attempt_id — update feedback on an accepted query.
 */
export const updateFeedback = <ThrowOnError extends boolean = false>(options: Options<UpdateFeedbackData, ThrowOnError>) => (options.client ?? client).patch<UpdateFeedbackResponses, UpdateFeedbackErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/feedback/{attempt_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List History
 *
 * GET /history — list accepted queries for the caller.
 *
 * Requires ``query.history.view`` permission. Filters by
 * ``user_id = current_user.id`` (FR-134 / SC-053). The optional
 * ``search`` parameter performs a case-insensitive literal substring
 * match over the caller's own question text and generated SQL; cursors
 * are bound to the active filter.
 */
export const listHistory = <ThrowOnError extends boolean = false>(options?: Options<ListHistoryData, ThrowOnError>) => (options?.client ?? client).get<ListHistoryResponses, ListHistoryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/history',
    ...options
});

/**
 * Delete History Entry
 *
 * DELETE /history/{id} — delete a single saved query result.
 *
 * Requires ``query.history.view`` permission. Filters by
 * ``user_id = current_user.id`` (FR-134 / SC-053); a query
 * belonging to another user returns 404.
 */
export const deleteHistoryEntry = <ThrowOnError extends boolean = false>(options: Options<DeleteHistoryEntryData, ThrowOnError>) => (options.client ?? client).delete<DeleteHistoryEntryResponses, DeleteHistoryEntryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/history/{query_id}',
    ...options
});

/**
 * Get History Entry
 *
 * GET /history/{id} — single accepted query detail.
 *
 * Requires ``query.history.view`` permission. Filters by
 * ``user_id = current_user.id`` (FR-134 / SC-053); a query
 * belonging to another user returns 404.
 */
export const getHistoryEntry = <ThrowOnError extends boolean = false>(options: Options<GetHistoryEntryData, ThrowOnError>) => (options.client ?? client).get<GetHistoryEntryResponses, GetHistoryEntryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/history/{query_id}',
    ...options
});

/**
 * Accept Query
 *
 * POST /query/accept — persist the current result.
 *
 * Requires ``query.submit`` permission.
 *
 * The service is rebuilt from immutable connection context in the Redis attempt.
 */
export const acceptQuery = <ThrowOnError extends boolean = false>(options: Options<AcceptQueryData, ThrowOnError>) => (options.client ?? client).post<AcceptQueryResponses, AcceptQueryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/query/accept',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Query Limits
 *
 * Return the configured question limit for authorized submitters.
 */
export const getQueryLimits = <ThrowOnError extends boolean = false>(options?: Options<GetQueryLimitsData, ThrowOnError>) => (options?.client ?? client).get<GetQueryLimitsResponses, GetQueryLimitsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/query/limits',
    ...options
});

/**
 * Regenerate Query
 *
 * POST /query/regenerate — regenerate SQL with negative context.
 *
 * Requires ``query.submit`` permission.
 */
export const regenerateQuery = <ThrowOnError extends boolean = false>(options: Options<RegenerateQueryData, ThrowOnError>) => (options.client ?? client).post<RegenerateQueryResponses, RegenerateQueryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/query/regenerate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reject Query
 *
 * POST /query/reject — reject current result and trigger auto-retry.
 *
 * Requires ``query.submit`` permission.
 */
export const rejectQuery = <ThrowOnError extends boolean = false>(options: Options<RejectQueryData, ThrowOnError>) => (options.client ?? client).post<RejectQueryResponses, RejectQueryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/query/reject',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Submit Question
 *
 * POST /query/submit — ask a question.
 *
 * Requires ``query.submit`` permission.
 *
 * Response shapes:
 * - 200 → QueryResult (returned directly)
 * - 422 → EvaluatorRejection (raised as HTTPException, unwrapped by global handler)
 * response_model is intentionally omitted because the endpoint returns
 * distinct success and evaluator-rejection shapes. The runtime OpenAPI
 * contract documents both without changing the response behavior.
 */
export const submitQuestion = <ThrowOnError extends boolean = false>(options: Options<SubmitQuestionData, ThrowOnError>) => (options.client ?? client).post<SubmitQuestionResponses, SubmitQuestionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/query/submit',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Sessions
 *
 * Return one stable keyset page of sessions owned by the current user.
 */
export const getSessions = <ThrowOnError extends boolean = false>(options?: Options<GetSessionsData, ThrowOnError>) => (options?.client ?? client).get<GetSessionsResponses, GetSessionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/sessions',
    ...options
});

/**
 * Create Session
 *
 * POST /sessions — create a new session.
 */
export const createSession = <ThrowOnError extends boolean = false>(options?: Options<CreateSessionData, ThrowOnError>) => (options?.client ?? client).post<CreateSessionResponses, CreateSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/sessions',
    ...options
});

/**
 * Delete Session
 *
 * DELETE /sessions/:id — delete a session (cascade deletes accepted_queries).
 */
export const deleteSession = <ThrowOnError extends boolean = false>(options: Options<DeleteSessionData, ThrowOnError>) => (options.client ?? client).delete<DeleteSessionResponses, DeleteSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/sessions/{session_id}',
    ...options
});

/**
 * Get Session
 *
 * GET /sessions/:id — get session detail with conversation history.
 */
export const getSession = <ThrowOnError extends boolean = false>(options: Options<GetSessionData, ThrowOnError>) => (options.client ?? client).get<GetSessionResponses, GetSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/sessions/{session_id}',
    ...options
});

/**
 * Update Session Connection
 *
 * PATCH /sessions/{id}/connection — update session's selected database (T-434, FR-094).
 *
 * Validates the connection is active + healthy + introspected before updating.
 */
export const updateSessionConnection = <ThrowOnError extends boolean = false>(options: Options<UpdateSessionConnectionData, ThrowOnError>) => (options.client ?? client).patch<UpdateSessionConnectionResponses, UpdateSessionConnectionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'session_id',
            type: 'apiKey'
        }],
    url: '/api/v1/sessions/{session_id}/connection',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Liveness Probe
 */
export const getHealth = <ThrowOnError extends boolean = false>(options?: Options<GetHealthData, ThrowOnError>) => (options?.client ?? client).get<GetHealthResponses, unknown, ThrowOnError>({ url: '/health', ...options });

/**
 * Readiness Probe
 */
export const getReadiness = <ThrowOnError extends boolean = false>(options?: Options<GetReadinessData, ThrowOnError>) => (options?.client ?? client).get<GetReadinessResponses, GetReadinessErrors, ThrowOnError>({ url: '/ready', ...options });
