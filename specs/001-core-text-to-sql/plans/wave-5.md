# Wave 5 Plan — US-4 (History + Filters)

Plan generated by Kimi K2.6 in Chunk 5.1.
main HEAD at planning: `566c93d`
Constitution version: v1.1.1
Phase: 1 of 6

## Goal

Close US-4 ("I can see prior accepted queries with filters") for Phase 1.

## Scope

### In-scope tasks (IN-SCOPE-PHASE-1)

| T-ID | Layer | Closes | Chunk |
|---|---|---|---|
| T-161 | backend test | FR-021,FR-023 | 5.2 |
| T-161b | backend test | FR-021 | 5.2 |
| T-162 | frontend test | FR-021,FR-023 | 5.3 |
| T-163 | frontend | FR-021,FR-022,FR-023 | 5.3 |
| T-164 | frontend test | FR-021,FR-022,SC-006,SC-007,SC-009 | 5.3 |
| T-165 | frontend | FR-021,FR-022,SC-006,SC-007,SC-009 | 5.3 |
| T-166 | frontend test | FR-023,SC-009 | 5.4 |
| T-167 | frontend | FR-023,SC-009 | 5.4 |
| T-168 | frontend test | FR-021,FR-022,FR-023,SC-009 | 5.4 |
| T-169 | frontend | FR-021,FR-022,FR-023,SC-009 | 5.4 |
| T-171 | E2E test | FR-021,FR-022,FR-023,SC-006,SC-007 | 5.5 |
| T-172 | E2E test | FR-021 | 5.5 |
| T-173 | E2E test | FR-020,SC-012 | 5.5 |

### Deferred tasks

| T-ID | Reason | Target phase |
|---|---|---|
| T-237 | Server-side history filtering deferred per FR-022 (client-side in Phase 1) | Phase 4+ |

### New tasks registered

| T-ID | Layer | Description | Chunk |
|---|---|---|---|
| T-238 | frontend [docs] | Add missing history i18n keys (`history.filter.placeholder`, `history.detail.question`, `history.detail.sql`, `history.detail.acceptedAt`) to `en.json` | 5.3 |
| T-239 | backend | Populate `HistoryListResponse.total` on first-page requests (no cursor) to satisfy T-161b contract | 5.2 |

### Polish residuals rolled into Wave 5

| T-ID | Description | Chunk |
|---|---|---|
| T-191c | Reconcile dual default on `accepted_queries.accepted_at` | 5.2 |
| T-211 | Document `X-Admin-Key` security scheme in `openapi.yaml` + `.env.example` | 5.2 |
| T-216 | Migrate i18n source-code references from `error.*` to `query.error.*` | 5.3 |
| T-221 | Document `HistoryListResponse.total` field in API contract | 5.2 |
| T-232 | Replace hardcoded `llm_provider="ollama"` default in `EphemeralAttempt` | 5.2 |
| T-234 | `accept_query` consistency: replace raw `redis.get` with `get_attempt()` | 5.2 |
| T-235 | Block server-metadata info-disclosure functions in `UnsafePatternRule` | 5.2 |
| T-236 | Remove `accept_query` dead-code field name fallback | 5.2 |

## Chunked implementation plan

### Chunk 5.2 — Backend history contract + repository polish
- Tasks: T-161, T-161b, T-239, T-191c, T-211, T-221, T-232, T-234, T-235, T-236
- Effort: ~45 min
- Key files:
  - `backend/src/app/services/history_service.py`
  - `backend/src/app/repositories/accepted_query_repository.py`
  - `backend/src/app/api/v1/history.py`
  - `backend/src/app/core/attempt_store.py`
  - `backend/src/app/evaluator/rules/unsafe_pattern_rule.py`
  - `specs/001-core-text-to-sql/contracts/openapi.yaml`
  - `backend/.env.example`
- Tests:
  - `backend/tests/contract/test_openapi_contract.py` (T-161 schemathesis extension)
  - `backend/tests/integration/test_history_total_field.py` (T-161b)

### Chunk 5.3 — Frontend useHistory hook + HistoryList component
- Tasks: T-162, T-163, T-164, T-165, T-238, T-216
- Effort: ~50 min
- Key files:
  - `frontend/src/hooks/useHistory.ts` (split from `useQuerySubmit.ts`)
  - `frontend/src/components/history/HistoryList.tsx`
  - `frontend/src/components/history/HistoryList.test.tsx`
  - `frontend/src/locales/en.json`
- Tests:
  - `frontend/tests/unit/useHistory.test.tsx` (T-162)
  - `frontend/tests/unit/HistoryList.test.tsx` (T-164)

### Chunk 5.4 — Frontend HistoryDetail + HistoryPage assembly
- Tasks: T-166, T-167, T-168, T-169
- Effort: ~45 min
- Key files:
  - `frontend/src/components/history/HistoryDetail.tsx`
  - `frontend/src/components/history/HistoryDetail.test.tsx`
  - `frontend/src/pages/HistoryPage.tsx`
  - `frontend/src/pages/HistoryPage.test.tsx`
- Tests:
  - `frontend/tests/unit/HistoryDetail.test.tsx` (T-166)
  - `frontend/tests/unit/HistoryPage.test.tsx` (T-168)

### Chunk 5.5 — E2E + Wave 5 snapshot
- Tasks: T-171, T-172, T-173, snapshot doc
- Effort: ~50 min
- Key files:
  - `frontend/tests/e2e/history.spec.ts`
- Tests: Playwright E2E against docker-compose.dev.yml

### Chunk 5.6 — Docs & infra polish residuals
- Tasks: T-211, T-216, T-221 (any docs not closed in 5.2)
- Effort: ~20 min
- Key files: openapi.yaml, .env.example, en.json

### Audit chunks (same pattern as Wave 4)
- Chunk 5.7: Gemini Pro 3.1 audit
- Chunk 5.8: Opus 4.6 audit
- Chunk 5.9: Triage + fixes + Wave 5 ships

## Foundation gates target

- Backend pytest (-m "not integration"): 271 passed → target 275+ (T-161b contract + total test)
- Frontend tests: 91 passed → target 110+ (useHistory, HistoryList, HistoryDetail, HistoryPage tests)
- E2E specs: 0 runnable (Playwright devDep missing) → target 3 runnable after T-210 pre-req

## Constitution alignment notes

- US-4 is Phase 1 (constitution §11). All in-scope work compatible with Phase 1 quality gates.
- Deferred: T-237 (server-side filtering) to Phase 4+ per FR-022 assumption.
- Deferred: T-233 (Inv 4 structural-equal review) to final polish — requires constitutional amendment.
- Deferred: all audit-log, RBAC, SSO, quota, and hostile-input work to Phase 5/6 per §11 phased commitment table.

## Risk register

- **Playwright devDep missing (T-210) blocks E2E execution**: T-210 is a Wave-5 pre-req. Mitigation: schedule Chunk 5.1b (T-210 only) before Chunk 5.5, or accept that E2E tests are written but not executed in CI until T-210 lands.
- **Frontend `useHistory` currently co-located in `useQuerySubmit.ts`**: Splitting it out is refactor risk. Mitigation: preserve existing exports while creating the new dedicated file.
- **HistoryList already has skeletal implementation**: Risk of test-task overlap. Mitigation: T-164/T-165 treat existing code as the implementation under test; tests must fail if filter or empty-state logic is removed.

## Open questions for orchestrator

1. Should T-210 (Playwright devDep) be promoted to a standalone Chunk 5.1b so E2E can execute before the final snapshot?
2. The existing `HistoryList.tsx` renders a table, not a scrollable list as described in T-165. Is the table layout acceptable for Phase 1, or should it be refactored to a list/card layout?
3. T-239 (populate `total`) is a small backend gap not covered by the original task slate. Does the orchestrator want it in Chunk 5.2 or handled as a side effect of T-161b?
