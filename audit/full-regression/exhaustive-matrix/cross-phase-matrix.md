# Cross-Phase Matrix

These rows capture current-contract changes, invariants, and regression paths that span more than one frozen phase.

| ID | Requirement / Task / Contract | User/API Path | Positive Cases | Negative / Edge Cases | Security / Privacy Checks | Existing Automated Coverage | Required Browser/API Real-Use Check | Evidence Needed | Status |
|---|---|---|---|---|---|---|---|---|---|
| XP-001 | Updated current contract: Phase 1 local-only auth is superseded by Phase 5 SSO for end users plus built-in admin local login. Sources: P1 FR-001, P5 FR-117..FR-121, FR-146. | `/sign-in`, SSO endpoints, `POST /auth/sign-in` | Built-in admin local login; mapped SSO user login. | Non-admin local login, unmapped SSO user. | No account enumeration or token leakage. | Phase 1 auth tests and Phase 5 SSO/local restriction tests. | Browser local admin and mocked/live SSO flow. | Auth matrix with status codes and screenshots. | Setup-dependent |
| XP-002 | Updated current contract: Phase 1 one PostgreSQL DB is superseded by Phase 3 centrally managed PG/MySQL/MSSQL connections with per-session selection. | `/admin/connections`, `/connections`, session connection patch | Single connection auto-selects; multiple require per-session selection. | No healthy connection, disabled/unhealthy connection. | Credentials absent from API/logs/frontend. | Phase 3 connection/query routing tests. | Browser add/select/query across DBs. | Connection API payloads and per-DB query screenshots. | Setup-dependent |
| XP-003 | Updated current contract: Phase 1 history became Phase 5 user-scoped history and accepted-query rerun revalidation. | `/history`, `/history/{id}`, rerun flow | User sees own accepted queries and reruns allowed by current role. | Cross-user direct ID, role restricted after acceptance. | No cross-user leakage. | Phase 1 history tests plus Phase 5 scoping/rerun tests. | Two-user API/browser history check. | Own/other history responses and rerun block evidence. | Covered by automated tests |
| XP-004 | Updated current contract: Phase 2/3/4 audit search/export deferrals are superseded by Phase 6 admin audit search/export/retention. | `/admin/audit/entries`, `/admin/audit/export`, `/admin/audit/retention` | Admin searches, exports, views retention. | Non-admin denied; export over 50k; quota exceeded. | Export redaction and formula injection prevention. | Phase 6 audit search/export/retention tests. | Browser UC-15..UC-17. | API/export samples and browser smoke. | Covered by browser/API full-regression evidence |
| XP-005 | Query pipeline ordering current invariant: hostile detection, then quota, then role/evaluator/LLM/source DB. Sources: P1 evaluator, P5 policy enforcement, P6 detection/quota tasks. | `POST /query/submit`, execute | Benign under quota reaches evaluator/LLM; safe SQL executes. | Hostile blocked; quota exceeded; disallowed schema; unsafe SQL. | Fail closed before sensitive side effects. | Phase 1 evaluator, Phase 5 policy, Phase 6 detection/quota tests. | API spy smoke for each short-circuit path. | Ordered call evidence and sanitized responses. | Covered by automated tests |
| XP-006 | LLM prompt privacy current invariant: selected schema is dialect-scoped by Phase 3 and role-filtered by Phase 5 before provider call. | Prompt builder/query submit | Allowed tables/columns and target dialect included. | Disallowed schema absent; no schema when no connection. | LLM never receives unauthorized schema. | Prompt builder and schema filtering tests. | Provider/mock prompt capture under restricted role. | Redacted prompt transcript. | Covered by automated tests |
| XP-007 | Read-only SQL invariant across all phases and dialects. | Evaluator/query execute | Read-only SELECT passes. | DML/DDL and unauthorized table/column rejected. | No source DB mutation. | Phase 1 evaluator and Phase 3/5 cross-dialect tests. | API unsafe SQL attempts per DB. | No-execution proof. | Covered by automated tests |
| XP-008 | Locale/RTL invariant expanded from Phase 1 foundations to Phase 2 activation, Phase 4 verification, Phase 5/6 new surfaces. | All user-facing surfaces | EN/AR parity and RTL mirror. | Missing keys, English fallback, physical CSS, mobile clipping. | Error strings sanitized before display. | locale coverage, CSS lint, RTL tests across phases. | Browser AR full sweep. | Locale output and screenshots. | Covered by automated tests |
| XP-009 | Mobile/responsive current contract: Phase 2 deferred full mobile shell, Phase 4 verifies responsive RTL, Phase 5 records two low mobile clipping deferrals, Phase 6 mobile smoke covers new surfaces. | Mobile 375px/768px | Phase 4/6 listed surfaces usable. | Phase 5 roles table actions and group mapping add button clip at 375px. | Clipping must not hide security-critical flows on desktop. | Phase 4/6 smoke; Phase 5 snapshot low deferrals. | Browser mobile sweep all admin/security surfaces. | Mobile screenshots and deferral validation. | Deferred by prior decision |
| XP-010 | Audit log integrity current contract: Phase 5 chain/immutability plus Phase 6 purge-gap verification and retention search/export. | Audit service/admin audit endpoints | Chain verifies before and after purge. | Missing marker reports tampering; export/search respects retention. | No secrets in stored/exported audit entries. | Phase 5/6 audit chain, immutability, purge tests. | API verify after seeded purge. | Chain status and audit entry samples. | Covered by automated tests |
| XP-011 | Permission model current contract: fixed role permissions gate UI and APIs; Phase 6 adds quota/security permissions. | All `/admin/*` routes | Authorized role succeeds. | Missing permission 403; legacy admin role-name bypass not accepted. | Least privilege. | permission gate tests and Phase 6 guard fixes. | Browser/API route matrix. | 403/success matrix by permission. | Covered by automated tests |
| XP-012 | Credential and secret redaction invariant across DB connections, SSO, audit, and export. | Connection APIs, SSO config, audit entries/export | Secrets accepted only on write. | Existing credential/cert/secret never redisplayed; export redacts sensitive stored values. | No passwords, tokens, certs, DB URLs, stack traces. | Phase 3 credential tests, Phase 5 audit redaction, Phase 6 export redaction. | API redaction sweep. | Redacted payloads and export samples. | Covered by automated tests |
| XP-013 | Redis dependency current paths: sessions, processing locks, quota counters. | Auth/session, query submit, quota-gated APIs | Redis healthy permits session and quota flow. | Redis unavailable causes auth/session or quota fail-closed behavior per path. | No unmetered quota path. | session/lock tests and quota fail-closed tests. | Service-degraded API smoke if approved. | 503/reauth responses and no side-effect proof. | Setup-dependent |
| XP-014 | Real LLM smoke remains higher fidelity than stub tests. Phase 1 Wave 7 found production-grade issues; Phase 2+ added contract tests. | Query submit with configured provider | Approved provider generates SQL and executes after checks. | Provider 429/5xx/malformed/oversized context. | Provider errors sanitized; prompts role-scoped. | provider contract tests and runbook live-smoke notes. | One approved live provider smoke per regression run if credentials exist. | Redacted provider transcript and query result. | Setup-dependent |
| XP-015 | External source DB smoke for PG/MySQL/MSSQL remains setup-dependent even where automated adapter coverage exists. | Admin connections, query submit/execute | Health, introspection, dialect query per DB. | Service unavailable, wrong credentials, schema failure. | No driver/credential leaks. | adapter/unit/integration tests; Phase 4 snapshot smoke. | Real local source DBs up and seeded. | Health, schema, SQL marker, result per DB. | Setup-dependent |
| XP-016 | Phase 6 audit gate current state permits T-905 but user explicitly prohibited starting T-905/freeze in this task. | Orchestrator docs only | Matrix created. | No freeze metadata update. | Prevent accidental phase-boundary mutation. | Wave 18 consolidation has 0 Critical/High, T-904 skipped. | None for matrix creation. | This matrix and PR only. | Deferred by prior decision |
| XP-017 | Browser evidence exists but must be refreshed for the target full-regression HEAD before claiming pass. | Existing phase evidence and audit/wave-18 screenshots | Evidence reviewed as source. | Stale screenshot or dirty artifact not staged. | Do not stage old screenshots/traces. | Phase snapshots and browser-smoke reports. | Prompt 2 rerun creates fresh evidence if approved. | New run folder report, not this matrix. | Needs manual/live test |
| XP-018 | Foundation gates are required after approved full regression, but not run during this matrix-only task. | Backend/frontend gate commands in runbook | Gates pass when executed. | Backend/frontend test/lint/build failure stops run. | Gates include `git diff --check`. | Prior snapshots record passed gates. | Prompt 2 execution. | Chunk reports with command exit codes. | Needs manual/live test |

